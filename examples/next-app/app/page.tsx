export default function Page() {
  return (
    <main style={{ padding: 24, fontFamily: 'ui-sans-serif, system-ui' }}>
      <h1>trpc-studio — Next.js example</h1>
      <p>
        Try mounting the studio at <code>/trpc-studio</code> once you've installed <code>@trpc-studio/next</code>.
      </p>
      <p>
        The tRPC endpoint is available at <code>/api/trpc</code>.
      </p>
    </main>
  );
}
